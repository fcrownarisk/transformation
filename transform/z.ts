abstract class Complex{
    constructor(public real: number, public imaginary: number) {}
   }
function zTransform(signal: number[], z: Complex){
    const result: Complex[] = [];
    for (let i = 0; i < signal.length; i++) {
       result.push()
    return result;
    }   
}